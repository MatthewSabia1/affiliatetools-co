---
import BaseLayout from '@/layouts/BaseLayout.astro';
import Hero from '@/components/Hero.astro';
import CTA from '@/components/CTA.astro';
import ToolLogo from '@/components/ToolLogo.astro';
import Rating from '@/components/Rating.astro';
import FAQ from '@/components/FAQ.astro';
import ScoringMethodology from '@/components/ScoringMethodology.astro';
import { getCompetitorsInDisplayOrder, overallScore } from '@/lib/competitors';
import { routes, SITE_URL, SITE_DESCRIPTION, VERSUS_SLUGS } from '@/lib/constants';
import { collectionPageJsonLd, breadcrumbList, faqPage } from '@/lib/schema';

const competitors = await getCompetitorsInDisplayOrder();

const flagshipListicles = [
  { slug: 'best-ad-spy-tools-2026', title: 'Best Ad Spy Tools of 2026', blurb: 'Our flagship ranking — tested across 8 dimensions.' },
  { slug: 'best-ad-spy-tools-for-affiliate-marketers', title: 'Best Ad Spy Tools for Affiliate Marketers', blurb: 'Affiliate-specific picks with CPA-network classification in mind.' },
  { slug: 'best-ad-spy-tools-for-facebook-ads', title: 'Best Ad Spy Tools for Facebook Ads', blurb: 'Meta-focused picks for Facebook & Instagram research.' },
  { slug: 'adspy-alternatives', title: 'AdSpy Alternatives', blurb: 'Better-value alternatives to the $149/mo incumbent.' },
  { slug: 'best-free-ad-spy-tools', title: 'Best Free Ad Spy Tools', blurb: 'What actually works at $0 — and when to upgrade.' },
  { slug: 'best-ad-spy-tools-for-clickbank', title: 'Best Ad Spy Tools for ClickBank', blurb: 'Tools that auto-detect ClickBank creatives vs those that don\'t.' },
];

const faqs = [
  { q: 'Who operates AffiliateTools.co?', a: 'AffiliateTools.co is operated by the AdRecon team — AdRecon is an ad intelligence platform for affiliate marketers on Meta. AdRecon is a direct competitor to most of the tools we review. We disclose this openly on every page and compensate for it by publishing our methodology, stating competitor strengths before our own, and admitting AdRecon\'s limitations (Meta-only coverage, no AI features, premium pricing).' },
  { q: 'What category is "affiliate tools"?', a: 'The affiliate marketer\'s software stack includes ad spy tools (for researching what\'s working), landing page builders, affiliate tracking platforms, offer networks, email marketing tools, and creative research tools. Our v1 coverage focuses on ad spy tools; we plan to expand into landing page builders, tracking platforms, and email tools over time.' },
  { q: 'Are these reviews paid placements?', a: 'No. We do not accept paid placement or sponsored rankings. AdRecon pays for the infrastructure hosting this site, but no tool on this site paid to be included or to improve its ranking. See our methodology for the ranking criteria we actually use.' },
  { q: 'How often are reviews updated?', a: 'We refresh pricing quarterly and rerun full feature audits annually. Every page shows the "Updated" date in the hero. If you spot an outdated fact, please email hello@affiliatetools.co.' },
  { q: 'Does AffiliateTools.co cover tools beyond ad spy?', a: 'Not yet in v1. The current scope is ad spy / ad intelligence tools for affiliate marketers on Meta. Future expansion will cover email marketing, affiliate networks, landing page builders, and tracking platforms.' },
];

const canonical = '/';
const title = 'AffiliateTools.co — Honest Reviews of Ad Spy Tools & Affiliate Software';
const description = SITE_DESCRIPTION;

const jsonLd = [
  breadcrumbList([{ name: 'Home', path: '/' }]),
  collectionPageJsonLd({
    pageUrl: `${SITE_URL}/`,
    name: 'AffiliateTools.co',
    description,
    hasPart: competitors.slice(0, 7).map((c) => ({
      url: `${SITE_URL}${routes.review(c.data.slug as any)}`,
      name: `${c.data.name} Review`,
      description: c.data.cardBlurb,
    })),
  }),
  faqPage(faqs),
];

const DIM_NAMES: Record<string, string> = {
  coverage: 'Coverage',
  dataFreshness: 'Freshness',
  price: 'Value',
  ux: 'UX',
  landingPages: 'Landing pages',
  offers: 'Offers',
  affiliateFocus: 'Affiliate fit',
  search: 'Search',
};

function startLabelFor(c: typeof competitors[number]['data']): string {
  if (c.pricing.startingMonthly === null) {
    return c.slug === 'adrecon' ? '$299 lifetime' : 'Custom pricing';
  }
  if (c.pricing.startingMonthly === 0) return 'Free tier';
  return `From $${c.pricing.startingMonthly}/mo`;
}

function topThreeDims(scores: Record<string, number>): Array<[string, number]> {
  return Object.entries(scores).sort((a, b) => b[1] - a[1]).slice(0, 3);
}

function firstBestFor(c: typeof competitors[number]['data']): string | null {
  return c.bestFor?.[0] ?? null;
}

function shortenBestFor(s: string): string {
  // Trim to a punchy 5-6 word chip. Break at common delimiters first, then fall back to word count.
  const cut = s.split(/[—–,:;(]/)[0]?.trim() ?? s;
  const words = cut.split(/\s+/);
  if (words.length <= 6) return cut;
  return words.slice(0, 6).join(' ') + '…';
}

// Pre-compute card data for all competitors.
const competitorCards = competitors.map((c, idx) => {
  const scoresRec: Record<string, number> = c.data.scores as unknown as Record<string, number>;
  const overall = overallScore(c.data.scores);
  return {
    rank: idx + 1,
    slug: c.data.slug,
    name: c.data.name,
    tagline: c.data.tagline,
    blurb: c.data.cardBlurb,
    startLabel: startLabelFor(c.data),
    overall,
    topDims: topThreeDims(scoresRec),
    bestFor: (c.data.bestFor ?? []).slice(0, 2).map(shortenBestFor),
    standout: c.data.standoutFeature?.trim(),
    href: routes.review(c.data.slug as any),
  };
});

// Sort competitors by overall score for the leaderboard (top 3)
const leaderboard = [...competitorCards].sort((a, b) => b.overall - a.overall).slice(0, 3);

// Directory leaderboard accent classes cycle for visual rhythm
const leaderAccents = ['var(--color-accent)', '#1f7a3e', '#b5791f'];
---

<BaseLayout title={title} description={description} canonical={canonical} jsonLd={jsonLd} pageType="website">
  <Hero
    eyebrow="Independent review hub · Updated April 2026"
    title="The software affiliate marketers actually run — reviewed honestly."
    sub="Hands-on reviews of ad spy tools, landing-page builders, affiliate networks, and the software that runs real affiliate campaigns. Operated by the AdRecon team — we disclose that openly."
    primaryCta={{ href: '/ad-spy-tools/', label: 'Browse ad spy tools' }}
    secondaryCta={{ href: '/methodology/', label: 'How we test' }}
    withStats={true}
  />

  {/* ========== TRUST STRIP ========== */}
  <section class="trust-strip" aria-label="At-a-glance editorial facts">
    <div class="container-wide trust-strip__inner">
      <div class="trust-kpi">
        <span class="trust-kpi__val">7</span>
        <span class="trust-kpi__key">Ad spy tools reviewed hands-on</span>
      </div>
      <div class="trust-kpi">
        <span class="trust-kpi__val">300K+</span>
        <span class="trust-kpi__key">Ad records analyzed on Meta</span>
      </div>
      <div class="trust-kpi">
        <span class="trust-kpi__val">8</span>
        <span class="trust-kpi__key">Scoring dimensions, individually weighted</span>
      </div>
      <div class="trust-kpi">
        <span class="trust-kpi__val">0</span>
        <span class="trust-kpi__key">Paid placements, ever</span>
      </div>
      <div class="trust-kpi trust-kpi--cta">
        <a href="/methodology/" class="trust-kpi__link">
          Read methodology
          <svg width="12" height="12" viewBox="0 0 12 12" fill="none" aria-hidden="true"><path d="M3 6h6m0 0L6 3m3 3L6 9" stroke="currentColor" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </a>
      </div>
    </div>
  </section>

  {/* ========== INTRO ========== */}
  <section class="container-wide section-intro">
    <div class="intro-grid">
      <div class="intro-prose">
        <p class="kicker"><span class="kicker__num">00</span> Editorial note</p>
        <p class="intro-lead">
          Affiliate marketers buy their tools differently than brand marketers. You need specificity —
          tools that understand ClickBank creatives, that track days-running instead of impressions,
          that capture landing pages before they get pulled. Most review sites don't know the difference.
        </p>
        <p>
          AffiliateTools.co is built by operators for operators. We review ad spy tools, landing-page
          platforms, affiliate networks, tracking stacks, and the broader software set that actually
          runs affiliate campaigns. Every review admits where a competitor wins before we pitch our own
          tool. Every claim is dated. Every price cites the source.
        </p>
      </div>
      <aside class="intro-aside">
        <h3 class="intro-aside__title">Quick picks</h3>
        <ul class="quick-picks">
          <li><span class="qp-tag">Meta affiliates</span><a href="/ad-spy-tools/adrecon-review/">AdRecon</a></li>
          <li><span class="qp-tag">Boolean search</span><a href="/ad-spy-tools/adspy-review/">AdSpy</a></li>
          <li><span class="qp-tag">Free tier</span><a href="/ad-spy-tools/bigspy-review/">BigSpy</a></li>
          <li><span class="qp-tag">TikTok</span><a href="/ad-spy-tools/pipiads-review/">PiPiADS</a></li>
          <li><span class="qp-tag">Dropshipping</span><a href="/ad-spy-tools/minea-review/">Minea</a></li>
          <li><span class="qp-tag">Agency workflow</span><a href="/ad-spy-tools/foreplay-review/">Foreplay</a></li>
        </ul>
      </aside>
    </div>
  </section>

  {/* ========== LEADERBOARD (top 3) ========== */}
  <section class="container-wide section-block">
    <header class="section-head">
      <p class="kicker"><span class="kicker__num">01</span> Leaderboard</p>
      <div class="section-head__titles">
        <h2>Top 3 ad spy tools this quarter</h2>
        <p class="section-head__sub">Ranked by aggregate score across 8 weighted dimensions.<br/>See the <a href="/methodology/">full methodology</a> or jump to any <a href="#featured-reviews">review below</a>.</p>
      </div>
    </header>
    <ol class="board">
      {leaderboard.map((card, i) => (
        <li class="board__row" style={`--accent: ${leaderAccents[i]};`}>
          <a href={card.href} class="board__link">
            <span class="board__rank">
              <span class="board__rank-num">0{i + 1}</span>
              <span class="board__rank-bar" aria-hidden="true"></span>
            </span>
            <span class="board__logo">
              <ToolLogo slug={card.slug as any} size="md" surface="none" />
            </span>
            <span class="board__title">
              <span class="board__name">{card.name}</span>
              <span class="board__tag">{card.tagline}</span>
            </span>
            <span class="board__score">
              <Rating value={card.overall} size="md" theme="light" />
            </span>
            <span class="board__price">
              <span class="board__price-val">{card.startLabel}</span>
              <span class="board__price-key">starting</span>
            </span>
            <span class="board__cta" aria-hidden="true">
              Open review
              <svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 7h8m0 0L7 3m4 4l-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
            </span>
          </a>
        </li>
      ))}
    </ol>
  </section>

  {/* ========== CATEGORIES ========== */}
  <section class="container-wide section-block">
    <header class="section-head section-head--compact">
      <p class="kicker"><span class="kicker__num">02</span> Categories</p>
      <div class="section-head__titles">
        <h2>What we cover</h2>
        <p class="section-head__sub">Starting with ad spy — expanding into the wider affiliate software stack.</p>
      </div>
    </header>
    <div class="cat-grid">
      <a class="cat-card cat-card--live" href="/ad-spy-tools/">
        <div class="cat-card__head">
          <div class="cat-card__icon">⬢</div>
          <span class="cat-card__pill cat-card__pill--live">Live</span>
        </div>
        <h3>Ad Spy Tools</h3>
        <p>7 reviews, 21 head-to-heads, and ranked listicles for affiliate marketers, agencies, and performance buyers.</p>
        <span class="cat-card__meta">7 reviews · 6 comparisons · 6 rankings</span>
      </a>
      <div class="cat-card cat-card--soon">
        <div class="cat-card__head">
          <div class="cat-card__icon">◇</div>
          <span class="cat-card__pill">Soon</span>
        </div>
        <h3>Landing Page Builders</h3>
        <p>ClickFunnels, Leadpages, Unbounce, Systeme.io — reviewed for affiliate marketers specifically.</p>
        <span class="cat-card__meta">Q3 2026 · 6+ reviews planned</span>
      </div>
      <div class="cat-card cat-card--soon">
        <div class="cat-card__head">
          <div class="cat-card__icon">⌗</div>
          <span class="cat-card__pill">Soon</span>
        </div>
        <h3>Tracking & Analytics</h3>
        <p>Voluum, RedTrack, Binom, FunnelFlux, Everflow — what real affiliates actually run in production.</p>
        <span class="cat-card__meta">Q4 2026 · 5+ reviews planned</span>
      </div>
      <div class="cat-card cat-card--soon">
        <div class="cat-card__head">
          <div class="cat-card__icon">✉</div>
          <span class="cat-card__pill">Soon</span>
        </div>
        <h3>Email Marketing</h3>
        <p>Beehiiv, ConvertKit, Klaviyo — scored against affiliate use cases specifically, not brand marketing.</p>
        <span class="cat-card__meta">2027 · 4+ reviews planned</span>
      </div>
    </div>
  </section>

  {/* ========== FEATURED REVIEWS ========== */}
  <section id="featured-reviews" class="container-wide section-block">
    <header class="section-head">
      <p class="kicker"><span class="kicker__num">03</span> Reviews</p>
      <div class="section-head__titles">
        <h2>Every ad spy tool, reviewed hands-on</h2>
        <p class="section-head__sub">Each tool tested against 8 weighted dimensions over 14-day trial windows. No sponsored placements. No fabricated testimonials.</p>
      </div>
    </header>
    <div class="review-grid">
      {competitorCards.map((card) => (
        <a class="rev" href={card.href}>
          <div class="rev__top">
            <span class="rev__rank">REVIEW · 0{card.rank}</span>
            <span class="rev__price">{card.startLabel}</span>
          </div>

          <div class="rev__logo-plate">
            <ToolLogo slug={card.slug as any} size="md" surface="none" />
          </div>

          <div class="rev__head">
            <h3 class="rev__name">{card.name}</h3>
            <p class="rev__tag">{card.tagline}</p>
          </div>

          <div class="rev__score-row">
            <div class="rev__score-block">
              <Rating value={card.overall} size="md" theme="dark" showLabel label="overall" />
            </div>
            <ul class="rev__dims">
              {card.topDims.map(([k, v]) => (
                <li class="rev__dim">
                  <span class="rev__dim-name">{DIM_NAMES[k]}</span>
                  <span class="rev__dim-track" aria-hidden="true">
                    <span class="rev__dim-fill" style={`width: ${v * 10}%;`}></span>
                  </span>
                  <span class="rev__dim-val">{v.toFixed(1)}</span>
                </li>
              ))}
            </ul>
          </div>

          {card.bestFor.length > 0 && (
            <div class="rev__chips">
              <span class="rev__chips-key">Best for</span>
              <span class="rev__chips-list">
                {card.bestFor.map((b) => <span class="rev__chip">{b}</span>)}
              </span>
            </div>
          )}

          <div class="rev__foot">
            <span class="rev__cta">
              Read the review
              <svg width="12" height="12" viewBox="0 0 14 14" fill="none" aria-hidden="true"><path d="M3 7h8m0 0L7 3m4 4l-4 4" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></svg>
            </span>
          </div>
        </a>
      ))}
    </div>
  </section>

  {/* ========== EDITORS BYLINE BAND ========== */}
  <section class="container-wide section-block">
    <div class="editors-band">
      <div class="editors-band__left">
        <p class="kicker"><span class="kicker__num">04</span> Who writes these reviews</p>
        <h2 class="editors-band__title">Operators, not bloggers.</h2>
        <p class="editors-band__body">
          AffiliateTools.co is run by the AdRecon team — people who actually buy ads, build landers, and
          cash affiliate checks. We built AdRecon because we needed better research on our own campaigns.
          We write these reviews because our team reads competitor marketing all day and got tired of
          nobody saying when the emperor had no clothes.
        </p>
        <ul class="editors-band__list">
          <li><strong>10+ years</strong> combined affiliate operator experience (ClickBank, Digistore24, native arb, DTC).</li>
          <li><strong>Full disclosure</strong> on every page: we own AdRecon, we say so, and we show its weaknesses before its strengths.</li>
          <li><strong>Quarterly refresh</strong> cadence on pricing and feature audits — annual on full methodology.</li>
        </ul>
      </div>
      <aside class="editors-band__right">
        <div class="authors">
          <div class="author">
            <span class="author__avatar" style="--a: #0e2c5f;">MS</span>
            <div class="author__body">
              <span class="author__name">Matthew Sabia</span>
              <span class="author__role">Founder · AdRecon · MediaGlobe</span>
            </div>
          </div>
          <div class="author">
            <span class="author__avatar" style="--a: #1f7a3e;">AT</span>
            <div class="author__body">
              <span class="author__name">AdRecon Team</span>
              <span class="author__role">Editorial · Testing · QA</span>
            </div>
          </div>
        </div>
        <div class="editors-band__badge">
          <span class="editors-band__badge-dot" aria-hidden="true"></span>
          <span>Editorial independence · No paid placement · Conflict disclosed</span>
        </div>
      </aside>
    </div>
  </section>

  <ScoringMethodology />

  {/* ========== HEAD-TO-HEADS ========== */}
  <section class="container-wide section-block">
    <header class="section-head section-head--compact">
      <p class="kicker"><span class="kicker__num">05</span> Comparisons</p>
      <div class="section-head__titles">
        <h2>Popular head-to-heads</h2>
        <p class="section-head__sub">AdRecon vs. every competitor we review — honest, side-by-side, with scorecards.</p>
      </div>
    </header>
    <div class="versus-grid">
      {VERSUS_SLUGS.map((slug) => {
        const c = competitors.find((x) => x.data.slug === slug);
        const tagline = c?.data.tagline ?? '';
        return (
          <a class="versus-card" href={routes.versus(slug)}>
            <span class="versus-card__kick">Head-to-head</span>
            <div class="versus-card__body">
              <span class="versus-card__a">AdRecon</span>
              <span class="versus-card__x" aria-hidden="true">vs</span>
              <span class="versus-card__b">{slug.charAt(0).toUpperCase() + slug.slice(1)}</span>
            </div>
            {tagline && <p class="versus-card__tag">{tagline}</p>}
            <span class="versus-card__cta">Compare →</span>
          </a>
        );
      })}
    </div>
  </section>

  {/* ========== LISTICLES ========== */}
  <section class="container-wide section-block">
    <header class="section-head section-head--compact">
      <p class="kicker"><span class="kicker__num">06</span> Rankings</p>
      <div class="section-head__titles">
        <h2>Best-of rankings</h2>
        <p class="section-head__sub">Ranked listicles across the most-asked queries. All picks sourced from the reviews above.</p>
      </div>
    </header>
    <div class="listicle-grid">
      {flagshipListicles.map((l, i) => (
        <a class="listicle-card" href={`/ad-spy-tools/${l.slug}/`}>
          <span class="listicle-card__num">0{i + 1}</span>
          <h3 class="listicle-card__title">{l.title}</h3>
          <p class="listicle-card__blurb">{l.blurb}</p>
          <span class="listicle-card__cta">Read ranking →</span>
        </a>
      ))}
    </div>
  </section>

  {/* ========== WHY ========== */}
  <section class="container-wide section-block">
    <header class="section-head section-head--compact">
      <p class="kicker"><span class="kicker__num">07</span> The receipts</p>
      <div class="section-head__titles">
        <h2>Why AffiliateTools.co</h2>
      </div>
    </header>
    <div class="why-grid">
      <div class="why-card">
        <div class="why-card__icon">◆</div>
        <h3>Operator-built</h3>
        <p>Run by the AdRecon team — affiliate marketers ourselves. We know the difference between a ClickBank creative and an e-commerce one.</p>
      </div>
      <div class="why-card">
        <div class="why-card__icon">◇</div>
        <h3>Meta-focused honesty</h3>
        <p>We publish AdRecon's limitations (Meta-only coverage, no AI features, premium pricing) openly. If another tool wins for your use case, we say so.</p>
      </div>
      <div class="why-card">
        <div class="why-card__icon">⬡</div>
        <h3>No paid placements</h3>
        <p>No tool paid to be included or ranked. We cite competitor marketing claims as claims — not facts — and date every price.</p>
      </div>
    </div>
  </section>

  <FAQ items={faqs} heading="Frequently asked questions" />

  <CTA
    variant="footer"
    campaign="home-footer"
    medium="home"
    label="Try AdRecon — $299 lifetime"
    kicker="Running Meta affiliate campaigns?"
    body="300,000+ ads, creatives, landing pages, and offers. Landing Page Ripper. Network classification for ClickBank, Digistore24, BuyGoods, MaxWeb, and WarriorPlus. The affiliate research stack."
  />
</BaseLayout>

<style>
  /* ========== SHARED EDITORIAL KICKER ========== */
  .kicker {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    font-family: var(--font-mono);
    font-size: 0.68rem;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--color-type-mute);
    font-weight: 600;
    margin: 0;
  }
  .kicker__num {
    font-variant-numeric: tabular-nums;
    color: var(--color-accent);
    padding-right: 0.55rem;
    margin-right: 0.3rem;
    border-right: 1px solid var(--color-line-2);
  }

  /* ========== TRUST STRIP ========== */
  .trust-strip {
    background: var(--color-canvas);
    border-top: 1px solid var(--color-line);
    border-bottom: 1px solid var(--color-line);
  }
  .trust-strip__inner {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 0;
    padding: 0;
  }
  .trust-kpi {
    display: flex;
    flex-direction: column;
    gap: 0.3rem;
    padding: 1.35rem 1.5rem;
    border-right: 1px solid var(--color-line);
    position: relative;
  }
  .trust-kpi:last-child { border-right: 0; }
  .trust-kpi__val {
    font-family: var(--font-sans);
    font-weight: 740;
    font-size: 1.65rem;
    color: var(--color-type);
    letter-spacing: -0.035em;
    line-height: 1;
    font-variant-numeric: tabular-nums;
  }
  .trust-kpi__key {
    font-size: 0.78rem;
    color: var(--color-type-mute);
    line-height: 1.35;
    max-width: 22ch;
  }
  .trust-kpi--cta { align-items: flex-start; justify-content: center; padding-top: 1.35rem; }
  .trust-kpi__link {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    font-family: var(--font-mono);
    font-size: 0.72rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    font-weight: 600;
    color: var(--color-accent);
    text-decoration: none;
    padding: 0.45rem 0.75rem;
    border: 1px solid var(--color-line-2);
    border-radius: 999px;
    transition: border-color .18s ease, color .18s ease, background .18s ease;
  }
  .trust-kpi__link:hover { border-color: var(--color-accent); background: var(--color-accent-softer); text-decoration: none; }
  @media (max-width: 980px) {
    .trust-strip__inner { grid-template-columns: repeat(2, 1fr); }
    .trust-kpi { border-bottom: 1px solid var(--color-line); }
    .trust-kpi:nth-child(2n) { border-right: 0; }
    .trust-kpi:nth-last-child(-n+2) { border-bottom: 0; }
    .trust-kpi--cta { grid-column: 1 / -1; }
  }

  /* ========== SECTIONS ========== */
  .section-intro { padding: 4rem 1.5rem 3rem; }
  .section-block { padding: 3.5rem 1.5rem; }

  .section-head {
    margin-bottom: 2rem;
    display: grid;
    grid-template-columns: 1fr;
    gap: 0.85rem;
    padding-bottom: 1.5rem;
    border-bottom: 1px solid var(--color-line);
  }
  .section-head--compact { padding-bottom: 1rem; }
  .section-head__titles { display: flex; flex-direction: column; gap: 0.5rem; }
  .section-head h2 { margin: 0; font-size: clamp(1.7rem, 2.4vw, 2.25rem); letter-spacing: -0.025em; line-height: 1.15; }
  .section-head__sub { color: var(--color-type-mute); font-size: 1.02rem; max-width: 58ch; margin: 0; line-height: 1.55; }
  @media (min-width: 900px) {
    .section-head {
      grid-template-columns: 180px 1fr;
      gap: 2rem;
      align-items: baseline;
    }
  }

  /* ========== INTRO ========== */
  .intro-grid {
    display: grid;
    grid-template-columns: 1.6fr 1fr;
    gap: 4rem;
    align-items: start;
  }
  @media (max-width: 900px) { .intro-grid { grid-template-columns: 1fr; gap: 2.5rem; } }

  .intro-prose .kicker { margin-bottom: 1rem; }
  .intro-lead {
    font-size: 1.3rem;
    color: var(--color-type);
    line-height: 1.5;
    letter-spacing: -0.015em;
    font-weight: 440;
  }
  .intro-prose p { font-size: 1.05rem; line-height: 1.65; }

  .intro-aside {
    background: var(--color-surface);
    border: 1px solid var(--color-line);
    border-radius: var(--radius-lg);
    padding: 1.5rem;
    position: sticky;
    top: 96px;
  }
  .intro-aside__title {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--color-type-mute);
    margin: 0 0 1rem;
  }
  .quick-picks { list-style: none; padding: 0; margin: 0; }
  .quick-picks li {
    display: grid;
    grid-template-columns: 110px 1fr;
    align-items: center;
    gap: 0.75rem;
    padding: 0.68rem 0;
    border-bottom: 1px solid var(--color-line);
    font-size: 0.96rem;
  }
  .quick-picks li:last-child { border-bottom: 0; }
  .quick-picks a { color: var(--color-type); font-weight: 600; text-decoration: none; }
  .quick-picks a:hover { color: var(--color-accent); }
  .qp-tag {
    font-family: var(--font-mono);
    font-size: 0.64rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--color-type-mute);
    font-weight: 500;
  }

  /* ========== LEADERBOARD ========== */
  .board {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    border: 1px solid var(--color-line);
    border-radius: var(--radius-lg);
    overflow: hidden;
    background: var(--color-canvas);
    box-shadow: var(--shadow-sm);
  }
  .board__row {
    border-bottom: 1px solid var(--color-line);
    margin: 0;
    position: relative;
  }
  .board__row:last-child { border-bottom: 0; }
  .board__row::before {
    content: "";
    position: absolute;
    left: 0;
    top: 0;
    bottom: 0;
    width: 4px;
    background: var(--accent);
    opacity: 0.9;
  }
  .board__link {
    display: grid;
    grid-template-columns: 80px 140px 1fr 140px 130px 140px;
    align-items: center;
    gap: 1.5rem;
    padding: 1.4rem 1.75rem 1.4rem 2rem;
    text-decoration: none;
    color: inherit;
    transition: background .15s ease;
  }
  .board__link:hover { background: var(--color-canvas-tint); text-decoration: none; }
  .board__rank {
    display: flex;
    flex-direction: column;
    gap: 0.35rem;
  }
  .board__rank-num {
    font-family: var(--font-mono);
    font-variant-numeric: tabular-nums;
    font-size: 1.1rem;
    font-weight: 700;
    color: var(--accent);
    letter-spacing: -0.01em;
    line-height: 1;
  }
  .board__rank-bar {
    display: block;
    width: 42px;
    height: 2px;
    background: var(--accent);
    opacity: 0.7;
    border-radius: 1px;
  }
  .board__logo {
    display: flex;
    align-items: center;
    justify-content: flex-start;
    min-height: 40px;
  }
  .board__title { display: flex; flex-direction: column; gap: 0.3rem; }
  .board__name {
    font-family: var(--font-sans);
    font-weight: 700;
    font-size: 1.18rem;
    letter-spacing: -0.02em;
    color: var(--color-type);
    line-height: 1.2;
  }
  .board__tag {
    font-size: 0.9rem;
    color: var(--color-type-mute);
    line-height: 1.4;
  }
  .board__score { display: flex; align-items: center; }
  .board__price { display: flex; flex-direction: column; gap: 0.25rem; align-items: flex-start; }
  .board__price-val {
    font-family: var(--font-mono);
    font-size: 0.92rem;
    font-weight: 600;
    color: var(--color-type);
    letter-spacing: -0.01em;
  }
  .board__price-key {
    font-size: 0.7rem;
    color: var(--color-type-mute);
    font-family: var(--font-mono);
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }
  .board__cta {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    font-size: 0.86rem;
    font-weight: 600;
    color: var(--color-accent);
    justify-self: end;
    letter-spacing: -0.005em;
    transition: gap .15s ease;
  }
  .board__link:hover .board__cta { gap: 0.6rem; }
  @media (max-width: 1080px) {
    .board__link {
      grid-template-columns: 56px 110px 1fr;
      row-gap: 0.85rem;
      padding: 1.25rem 1.25rem 1.25rem 1.5rem;
    }
    .board__score { grid-column: 2; grid-row: 2; }
    .board__price { grid-column: 3; grid-row: 2; align-items: flex-end; text-align: right; }
    .board__cta { grid-column: 1 / -1; justify-self: start; margin-top: 0.1rem; }
  }
  @media (max-width: 560px) {
    .board__link { grid-template-columns: 44px 1fr; gap: 1rem; padding: 1rem 1rem 1rem 1.25rem; }
    .board__logo { grid-column: 1 / -1; }
    .board__title { grid-column: 1 / -1; }
    .board__score { grid-column: 1 / -1; }
    .board__price { grid-column: 1 / -1; align-items: flex-start; text-align: left; }
  }

  /* ========== CATEGORIES ========== */
  .cat-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 1rem;
  }
  .cat-card {
    display: flex;
    flex-direction: column;
    gap: 0.65rem;
    padding: 1.6rem;
    background: var(--color-canvas);
    border: 1px solid var(--color-line);
    border-radius: var(--radius-lg);
    text-decoration: none;
    color: inherit;
    transition: border-color .18s ease, transform .18s ease, box-shadow .18s ease;
    box-shadow: var(--shadow-xs);
  }
  .cat-card--live:hover {
    transform: translateY(-2px);
    border-color: var(--color-accent);
    box-shadow: var(--shadow-md);
  }
  .cat-card--soon { opacity: 0.72; cursor: default; }
  .cat-card__head {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 0.2rem;
  }
  .cat-card__icon {
    font-size: 1.5rem;
    color: var(--color-accent);
    line-height: 1;
  }
  .cat-card__pill {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    padding: 0.2rem 0.55rem;
    border-radius: 999px;
    font-family: var(--font-mono);
    font-size: 0.64rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    font-weight: 600;
    background: var(--color-surface);
    border: 1px solid var(--color-line);
    color: var(--color-type-mute);
  }
  .cat-card__pill--live {
    background: var(--color-success-soft);
    border-color: color-mix(in oklch, var(--color-success) 30%, var(--color-line));
    color: var(--color-success);
  }
  .cat-card__pill--live::before {
    content: "";
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--color-success);
  }
  .cat-card h3 { margin: 0.2rem 0 0.3rem; font-size: 1.18rem; letter-spacing: -0.02em; }
  .cat-card p { margin: 0; font-size: 0.93rem; color: var(--color-type-mute); line-height: 1.55; }
  .cat-card__meta {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    color: var(--color-type-subtle);
    margin-top: 0.5rem;
    letter-spacing: 0.02em;
    padding-top: 0.75rem;
    border-top: 1px solid var(--color-line);
  }

  /* ========== DARK REVIEW CARDS ========== */
  .review-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: 1.1rem;
  }
  .rev {
    display: flex;
    flex-direction: column;
    padding: 1.75rem 1.75rem 1.5rem;
    background:
      radial-gradient(ellipse 90% 50% at 50% 0%, rgba(62, 116, 201, 0.14), transparent 60%),
      linear-gradient(180deg, #0f1a2e 0%, #080d17 100%);
    border: 1px solid rgba(255, 255, 255, 0.07);
    border-radius: 16px;
    color: #ffffff;
    text-decoration: none;
    position: relative;
    overflow: hidden;
    transition: transform .22s cubic-bezier(.2,.8,.2,1), box-shadow .22s ease, border-color .22s ease;
  }
  .rev::before {
    content: "";
    position: absolute;
    inset: 0 0 auto 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.14), transparent);
    pointer-events: none;
  }
  .rev:hover {
    transform: translateY(-4px);
    border-color: rgba(180, 198, 230, 0.22);
    box-shadow:
      0 30px 60px -24px rgba(6, 12, 22, 0.7),
      0 0 0 1px rgba(134, 166, 220, 0.12);
    text-decoration: none;
  }

  .rev__top {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 0.75rem;
    margin-bottom: 1.25rem;
  }
  .rev__rank {
    font-family: var(--font-mono);
    font-size: 0.64rem;
    letter-spacing: 0.16em;
    color: rgba(255, 255, 255, 0.42);
    text-transform: uppercase;
    font-weight: 600;
  }
  .rev__price {
    font-family: var(--font-mono);
    font-size: 0.72rem;
    color: rgba(255, 255, 255, 0.78);
    letter-spacing: -0.005em;
    padding: 0.22rem 0.65rem;
    border-radius: 999px;
    border: 1px solid rgba(255, 255, 255, 0.1);
    background: rgba(255, 255, 255, 0.04);
    white-space: nowrap;
    font-weight: 500;
  }

  .rev__logo-plate {
    background: #ffffff;
    border-radius: 12px;
    padding: 1rem 1.15rem;
    display: flex;
    align-items: center;
    justify-content: flex-start;
    min-height: 72px;
    box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.05);
    margin-bottom: 1.15rem;
  }

  .rev__head { margin-bottom: 1rem; }
  .rev__name {
    margin: 0;
    font-family: var(--font-sans);
    font-weight: 700;
    font-size: 1.32rem;
    color: #ffffff;
    letter-spacing: -0.025em;
    line-height: 1.18;
  }
  .rev__tag {
    margin: 0.38rem 0 0;
    color: rgba(255, 255, 255, 0.6);
    font-size: 0.93rem;
    line-height: 1.5;
  }

  .rev__score-row {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: 1.5rem;
    align-items: flex-start;
    padding: 1.1rem 0;
    margin: 0 0 1.1rem;
    border-top: 1px solid rgba(255, 255, 255, 0.07);
    border-bottom: 1px solid rgba(255, 255, 255, 0.07);
  }
  .rev__score-block { min-width: 4.4rem; }

  .rev__dims {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.44rem;
  }
  .rev__dim {
    display: grid;
    grid-template-columns: 88px 1fr 32px;
    align-items: center;
    gap: 0.65rem;
    font-size: 0.75rem;
  }
  .rev__dim-name {
    color: rgba(255, 255, 255, 0.52);
    letter-spacing: -0.005em;
    font-weight: 500;
  }
  .rev__dim-track {
    height: 3px;
    background: rgba(255, 255, 255, 0.08);
    border-radius: 2px;
    overflow: hidden;
  }
  .rev__dim-fill {
    display: block;
    height: 100%;
    background: linear-gradient(90deg, #8fb1ec 0%, #dbe7fb 100%);
    border-radius: 2px;
  }
  .rev__dim-val {
    font-family: var(--font-mono);
    font-weight: 600;
    color: rgba(255, 255, 255, 0.82);
    font-size: 0.74rem;
    text-align: right;
    font-variant-numeric: tabular-nums;
  }

  .rev__chips {
    display: flex;
    flex-direction: column;
    gap: 0.45rem;
    margin-bottom: 1.25rem;
  }
  .rev__chips-key {
    font-family: var(--font-mono);
    font-size: 0.62rem;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.42);
    font-weight: 600;
  }
  .rev__chips-list {
    display: flex;
    flex-wrap: wrap;
    gap: 0.35rem;
  }
  .rev__chip {
    display: inline-flex;
    align-items: flex-start;
    padding: 0.3rem 0.65rem;
    border-radius: 10px;
    border: 1px solid rgba(255, 255, 255, 0.12);
    background: rgba(255, 255, 255, 0.03);
    font-size: 0.72rem;
    color: rgba(255, 255, 255, 0.82);
    line-height: 1.35;
    font-weight: 500;
    letter-spacing: -0.005em;
    max-width: 100%;
    hyphens: auto;
  }

  .rev__foot {
    margin-top: auto;
    padding-top: 0.75rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-top: 1px dashed rgba(255, 255, 255, 0.08);
  }
  .rev__cta {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    color: #a8c5f5;
    font-weight: 600;
    font-size: 0.86rem;
    letter-spacing: -0.005em;
    transition: gap .15s ease, color .15s ease;
  }
  .rev:hover .rev__cta {
    gap: 0.6rem;
    color: #e0eafa;
  }

  /* ========== EDITORS BAND ========== */
  .editors-band {
    display: grid;
    grid-template-columns: 1.5fr 1fr;
    gap: 4rem;
    padding: 3rem;
    background:
      linear-gradient(180deg, var(--color-canvas-tint) 0%, var(--color-canvas) 100%);
    border: 1px solid var(--color-line);
    border-radius: var(--radius-xl);
    position: relative;
    overflow: hidden;
  }
  .editors-band::before {
    content: "";
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(to right, rgba(14, 44, 95, 0.03) 1px, transparent 1px),
      linear-gradient(to bottom, rgba(14, 44, 95, 0.03) 1px, transparent 1px);
    background-size: 48px 48px;
    mask-image: radial-gradient(ellipse at 100% 0%, black 0%, transparent 70%);
    -webkit-mask-image: radial-gradient(ellipse at 100% 0%, black 0%, transparent 70%);
    pointer-events: none;
  }
  .editors-band > * { position: relative; }
  @media (max-width: 960px) {
    .editors-band { grid-template-columns: 1fr; gap: 2rem; padding: 2rem; }
  }

  .editors-band .kicker { margin-bottom: 1rem; }
  .editors-band__title {
    margin: 0 0 0.85rem;
    font-size: clamp(1.8rem, 2.8vw, 2.4rem);
    letter-spacing: -0.03em;
    line-height: 1.1;
  }
  .editors-band__body {
    color: var(--color-type-dim);
    font-size: 1.05rem;
    line-height: 1.6;
    margin: 0 0 1.25rem;
    max-width: 54ch;
  }
  .editors-band__list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
  }
  .editors-band__list li {
    position: relative;
    padding: 0.4rem 0 0.4rem 1.4rem;
    font-size: 0.96rem;
    color: var(--color-type-dim);
    line-height: 1.5;
    border-bottom: 1px solid var(--color-line);
    margin: 0;
  }
  .editors-band__list li:last-child { border-bottom: 0; }
  .editors-band__list li::before {
    content: "";
    position: absolute;
    left: 0;
    top: 50%;
    width: 8px;
    height: 2px;
    background: var(--color-accent);
    transform: translateY(-50%);
  }

  .editors-band__right {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    align-items: flex-start;
  }
  .authors {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    width: 100%;
  }
  .author {
    display: flex;
    align-items: center;
    gap: 0.9rem;
    padding: 0.75rem;
    background: var(--color-canvas);
    border: 1px solid var(--color-line);
    border-radius: var(--radius-md);
    box-shadow: var(--shadow-xs);
  }
  .author__avatar {
    flex: 0 0 auto;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background: var(--a);
    color: #fff;
    font-family: var(--font-sans);
    font-weight: 700;
    font-size: 0.82rem;
    letter-spacing: 0.01em;
    box-shadow: 0 1px 3px rgba(11, 20, 32, 0.12), inset 0 0 0 1px rgba(255, 255, 255, 0.08);
  }
  .author__body { display: flex; flex-direction: column; gap: 0.15rem; }
  .author__name {
    font-weight: 650;
    color: var(--color-type);
    font-size: 0.95rem;
    letter-spacing: -0.01em;
  }
  .author__role {
    font-size: 0.78rem;
    color: var(--color-type-mute);
  }
  .editors-band__badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.6rem 0.9rem;
    border-radius: 999px;
    background: var(--color-canvas);
    border: 1px solid var(--color-line);
    font-family: var(--font-mono);
    font-size: 0.7rem;
    color: var(--color-type-dim);
    letter-spacing: 0.02em;
  }
  .editors-band__badge-dot {
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: var(--color-success);
    box-shadow: 0 0 0 3px rgba(31, 122, 62, 0.18);
  }

  /* ========== VERSUS CARDS ========== */
  .versus-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 0.85rem;
  }
  .versus-card {
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
    padding: 1.35rem 1.35rem 1.15rem;
    background: var(--color-canvas);
    border: 1px solid var(--color-line);
    border-radius: var(--radius-lg);
    text-decoration: none;
    color: inherit;
    transition: border-color .18s ease, transform .18s ease, box-shadow .18s ease;
    box-shadow: var(--shadow-xs);
  }
  .versus-card:hover {
    transform: translateY(-2px);
    border-color: var(--color-accent);
    box-shadow: var(--shadow-md);
    text-decoration: none;
  }
  .versus-card__kick {
    font-family: var(--font-mono);
    font-size: 0.64rem;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--color-accent);
    font-weight: 600;
  }
  .versus-card__body {
    display: flex;
    align-items: center;
    gap: 0.55rem;
    margin: 0.15rem 0 0.35rem;
  }
  .versus-card__a, .versus-card__b {
    font-family: var(--font-sans);
    font-weight: 700;
    font-size: 1.04rem;
    color: var(--color-type);
    letter-spacing: -0.015em;
  }
  .versus-card__x {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--color-type-mute);
    padding: 0.18rem 0.45rem;
    border: 1px solid var(--color-line-2);
    border-radius: 999px;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    font-weight: 500;
    background: var(--color-surface);
  }
  .versus-card__tag {
    margin: 0;
    font-size: 0.84rem;
    color: var(--color-type-mute);
    line-height: 1.4;
    min-height: 2.4em;
  }
  .versus-card__cta {
    color: var(--color-accent);
    font-weight: 600;
    font-size: 0.82rem;
    margin-top: 0.15rem;
  }

  /* ========== LISTICLES ========== */
  .listicle-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1rem;
  }
  .listicle-card {
    display: flex;
    flex-direction: column;
    gap: 0.55rem;
    padding: 1.75rem 1.5rem 1.35rem;
    background: var(--color-canvas);
    border: 1px solid var(--color-line);
    border-radius: var(--radius-lg);
    text-decoration: none;
    color: inherit;
    transition: border-color .18s ease, transform .18s ease, box-shadow .18s ease;
    box-shadow: var(--shadow-xs);
    position: relative;
    overflow: hidden;
  }
  .listicle-card:hover {
    transform: translateY(-2px);
    border-color: var(--color-accent);
    box-shadow: var(--shadow-md);
    text-decoration: none;
  }
  .listicle-card__num {
    position: absolute;
    top: 1rem;
    right: 1.25rem;
    font-family: var(--font-mono);
    font-size: 3rem;
    font-weight: 300;
    color: var(--color-line-2);
    letter-spacing: -0.03em;
    line-height: 1;
    pointer-events: none;
    transition: color .18s ease;
  }
  .listicle-card:hover .listicle-card__num { color: var(--color-accent-soft); }
  .listicle-card__title {
    margin: 0;
    font-size: 1.1rem;
    line-height: 1.3;
    letter-spacing: -0.02em;
    max-width: 20ch;
    position: relative;
  }
  .listicle-card__blurb {
    margin: 0;
    color: var(--color-type-mute);
    font-size: 0.92rem;
    line-height: 1.5;
    position: relative;
  }
  .listicle-card__cta {
    color: var(--color-accent);
    font-weight: 600;
    font-size: 0.85rem;
    margin-top: auto;
    padding-top: 0.85rem;
    border-top: 1px solid var(--color-line);
    position: relative;
  }

  /* ========== WHY ========== */
  .why-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1rem;
  }
  .why-card {
    padding: 1.8rem;
    background: var(--color-canvas);
    border: 1px solid var(--color-line);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-xs);
  }
  .why-card__icon { color: var(--color-accent); font-size: 1.5rem; line-height: 1; margin-bottom: 0.75rem; }
  .why-card h3 { margin: 0 0 0.5rem; font-size: 1.12rem; letter-spacing: -0.02em; }
  .why-card p { margin: 0; font-size: 0.95rem; color: var(--color-type-mute); line-height: 1.55; }
</style>
